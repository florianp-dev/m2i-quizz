@model IEnumerable<ModelEntities.Entities.Percent>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Beginner)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Intermediate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Expert)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Beginner)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Intermediate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Expert)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PercentID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PercentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PercentID })
        </td>
    </tr>
}

</table>
